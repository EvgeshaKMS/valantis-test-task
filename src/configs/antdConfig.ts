import { ThemeConfig } from 'antd';

export const antdConfig: ThemeConfig = {
  components: {
    Button: {
      fontSize: 16,
      onlyIconSizeLG: 22,
    },
    Form: {
      itemMarginBottom: 12,
    },
  },
};
